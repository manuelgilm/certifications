# A sample job for data_prep_for_ml.

resources:
  jobs:
    sample_job:
      name: sample_job

      # trigger:
      #   # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
      #   periodic:
      #     interval: 1
      #     unit: DAYS

      #email_notifications:
      #  on_failure:
      #    - your_email@example.com

      # parameters:
      #   - name: catalog
      #     default: ${var.catalog}
      #   - name: schema
      #     default: ${var.schema}
      tasks:
        - task_key: notebook_task
          notebook_task:
            notebook_path: ../src/sample_notebook.ipynb

          # job_cluster_key: job_cluster
        #   libraries:
        #     - whl: ../dist/*.whl
        # - task_key: python_wheel_task
        #   environment_key: serverless
        #   # depends_on:
        #   #   - task_key: notebook_task
        #   python_wheel_task:
        #     package_name: data_prep_for_ml
        #     entry_point: main
      environments:
        - environment_key: serverless
          spec:
            environment_version: 4
            dependencies:
              - ../dist/*.whl

